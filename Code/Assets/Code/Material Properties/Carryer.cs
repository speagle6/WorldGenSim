using UnityEngine;

public class Carryer : MonoBehaviour
{
    public int QuantPerQuant;
    // figure out how to add release criteria
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

using UnityEngine;

public class Erodable : MonoBehaviour
{
    // need to figure out how to handle requirements
    public Rigid RigToDis;
    public Loose LooseToAct;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
